{
  "name": "wexio-widget-example-web-component",
  "private": true,
  "version": "0.0.0",
  "description": "Wexio Widget — Web Component embed. Zero build, zero framework. A single <script type='module'> + <wexio-widget> tag.",
  "scripts": {
    "start": "serve .",
    "dev": "serve .",
    "build": "echo 'static — no build step'"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
